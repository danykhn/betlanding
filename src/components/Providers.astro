---
---

<section id="proveedores" class="relative py-24 bg-gradient-to-b from-slate-950 to-slate-900">
  <div class="container mx-auto px-6">
    <div class="text-center mb-16 transition-all duration-1000 taos:opacity-0 taos:scale-50" data-taos-offset="400">
      <h2 class="text-4xl md:text-5xl font-bold mb-4">
        Nuestros <span class="bg-gradient-to-r from-purple-500 to-pink-500 bg-clip-text text-transparent">Proveedores</span>
      </h2>
      <p class="text-xl text-gray-400 max-w-3xl mx-auto">
        Trabajamos con los mejores proveedores de la industria del igaming
      </p>
    </div>

    <!-- Infinite scroll carousel -->
    <div class="relative overflow-hidden">
      <div class="providers-track flex gap-8 animate-scroll">
        <!-- First set of providers -->
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Pragmatic Play</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Evolution</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Ezugi</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Playson</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Red Rake</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Evo Play</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">BetGames</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Kalamba</span>
        </div>
        
        <!-- Duplicate set for seamless loop -->
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Pragmatic Play</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Evolution</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Ezugi</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Playson</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Red Rake</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Evo Play</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">BetGames</span>
        </div>
        <div class="provider-card flex-shrink-0 w-48 h-32 bg-white rounded-xl flex items-center justify-center hover:scale-110 transition-transform duration-300">
          <span class="text-gray-800 font-bold text-lg">Kalamba</span>
        </div>
      </div>
    </div>

    <!-- Stats section with counter animation -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8 mt-20">
      <div class="stat-card text-center p-8 bg-slate-800/50 rounded-2xl border border-slate-700 hover:border-purple-500 transition-all duration-1000 taos:opacity-0 taos:translate-y-[200px]" data-taos-offset="300">
        <div class="text-5xl font-bold text-purple-500 mb-2 counter" data-target="5000">0</div>
        <div class="text-gray-400">Juegos Disponibles</div>
      </div>
      <div class="stat-card text-center p-8 bg-slate-800/50 rounded-2xl border border-slate-700 hover:border-pink-500 transition-all duration-1000 taos:opacity-0 taos:-translate-x-[200px]" data-taos-offset="300" data-taos-delay="200">
        <div class="text-5xl font-bold text-pink-500 mb-2 counter" data-target="50">0</div>
        <div class="text-gray-400">Proveedores</div>
      </div>
      <div class="stat-card text-center p-8 bg-slate-800/50 rounded-2xl border border-slate-700 hover:border-blue-500 transition-all duration-1000 taos:opacity-0 taos:scale-[0.3]" data-taos-offset="300" data-taos-delay="400">
        <div class="text-5xl font-bold text-blue-500 mb-2">24/7</div>
        <div class="text-gray-400">Soporte Técnico</div>
      </div>
      <div class="stat-card text-center p-8 bg-slate-800/50 rounded-2xl border border-slate-700 hover:border-indigo-500 transition-all duration-1000 taos:opacity-0 taos:translate-x-[200px]" data-taos-offset="300" data-taos-delay="600">
        <div class="text-5xl font-bold text-indigo-500 mb-2 counter" data-target="10">0</div>
        <div class="text-gray-400">Años de Experiencia</div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  .animate-scroll {
    animation: scroll 30s linear infinite;
  }

  .providers-track:hover {
    animation-play-state: paused;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }
</style>

<script>
  // Counter animation
  const observerOptions = {
    threshold: 0.5,
    rootMargin: '0px'
  };

  const animateCounter = (element: Element) => {
    const target = parseInt((element as HTMLElement).dataset.target || '0');
    const duration = 2000;
    const step = target / (duration / 16);
    let current = 0;

    const updateCounter = () => {
      current += step;
      if (current < target) {
        element.textContent = Math.floor(current).toString();
        requestAnimationFrame(updateCounter);
      } else {
        element.textContent = target.toString() + (target >= 1000 ? '+' : '');
      }
    };

    updateCounter();
  };

  const counterObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting && entry.target.classList.contains('counter')) {
        animateCounter(entry.target);
        counterObserver.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.querySelectorAll('.counter').forEach((counter) => {
    counterObserver.observe(counter);
  });
</script>
