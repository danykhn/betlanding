---
---

<section id="contacto" class="relative py-24 overflow-hidden">
  <!-- Background effects -->
  <div class="absolute inset-0 bg-gradient-to-b from-slate-950 via-purple-950/20 to-slate-950"></div>
  
  <div class="container mx-auto px-6 relative z-10">
    <div class="text-center mb-16 transition-all duration-1000 taos:opacity-0 taos:translate-y-[100px]" data-taos-offset="400">
      <h2 class="text-4xl md:text-5xl font-bold mb-4">
        Empezá ya tu operación de <span class="bg-gradient-to-r from-purple-500 to-pink-500 bg-clip-text text-transparent">casino online</span>
      </h2>
      <p class="text-xl text-gray-400 max-w-3xl mx-auto">
        Contactános y hagamos negocios juntos
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
      <!-- Contact Form -->
      <div class="bg-slate-800/50 backdrop-blur-sm rounded-3xl p-8 border border-slate-700 transition-all duration-1000 taos:opacity-0 taos:-translate-x-[300px]" data-taos-offset="300">
        <form id="contact-form" class="space-y-6">
          <div>
            <label for="name" class="block text-sm font-medium text-gray-300 mb-2">Nombre</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              required
              class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-300 text-white"
              placeholder="Tu nombre"
            />
          </div>

          <div>
            <label for="email" class="block text-sm font-medium text-gray-300 mb-2">Email</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              required
              class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-300 text-white"
              placeholder="tu@email.com"
            />
          </div>

          <div>
            <label for="phone" class="block text-sm font-medium text-gray-300 mb-2">Teléfono</label>
            <input 
              type="tel" 
              id="phone" 
              name="phone"
              class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-300 text-white"
              placeholder="+1 (786) 947-2308"
            />
          </div>

          <div>
            <label for="message" class="block text-sm font-medium text-gray-300 mb-2">Mensaje</label>
            <textarea 
              id="message" 
              name="message" 
              rows="4"
              required
              class="w-full px-4 py-3 bg-slate-900 border border-slate-700 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-300 text-white resize-none"
              placeholder="Cuéntanos sobre tu proyecto..."
            ></textarea>
          </div>

          <button 
            type="submit"
            class="w-full px-8 py-4 bg-gradient-to-r from-purple-600 to-pink-600 rounded-lg text-white font-semibold hover:shadow-2xl hover:shadow-purple-500/50 transition-all duration-300 transform hover:scale-105"
          >
            Enviar Mensaje
          </button>
        </form>
      </div>

      <!-- Contact Information -->
      <div class="space-y-8">
        <div class="bg-slate-800/50 backdrop-blur-sm rounded-3xl p-8 border border-slate-700 hover:border-purple-500 transition-all duration-1000 taos:opacity-0 taos:translate-x-[300px]" data-taos-offset="300">
          <div class="flex items-start space-x-4">
            <div class="text-3xl">📧</div>
            <div>
              <h3 class="text-xl font-semibold mb-2">Email</h3>
              <a href="mailto:contacto@betfounding.com" class="text-purple-400 hover:text-purple-300 transition-colors">
                contacto@betfounding.com
              </a>
            </div>
          </div>
        </div>

        <div class="bg-slate-800/50 backdrop-blur-sm rounded-3xl p-8 border border-slate-700 hover:border-pink-500 transition-all duration-1000 taos:opacity-0 taos:scale-[0.5]" data-taos-offset="300" data-taos-delay="200">
          <div class="flex items-start space-x-4">
            <div class="text-3xl">📱</div>
            <div>
              <h3 class="text-xl font-semibold mb-2">WhatsApp Directo</h3>
              <a href="https://wa.me/17869472308" class="text-pink-400 hover:text-pink-300 transition-colors" target="_blank">
                +1 (786) 947-2308
              </a>
            </div>
          </div>
        </div>

        <div class="bg-slate-800/50 backdrop-blur-sm rounded-3xl p-8 border border-slate-700 hover:border-blue-500 transition-all duration-1000 taos:opacity-0 taos:translate-y-[200px]" data-taos-offset="300" data-taos-delay="400">
          <div class="flex items-start space-x-4">
            <div class="text-3xl">🌍</div>
            <div>
              <h3 class="text-xl font-semibold mb-3">Nuestras Oficinas</h3>
              <div class="space-y-3 text-sm text-gray-400">
                <p>🇦🇷 Buenos Aires: Av. Del Libertador 8008, Oficina 29 03</p>
                <p>🇺🇸 Miami: 150 SE 2nd Ave 3rd floor, FL 33131</p>
                <p>🇻🇪 Barquisimeto: Av. Los Leones & Avenida Caroní, Lara</p>
                <p>🇪🇨 Quito: Av. La Coruña E12-148 Y Valladolid Coworking</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Team Preview -->
        <div class="bg-gradient-to-br from-purple-900/30 to-pink-900/30 rounded-3xl p-8 border border-purple-500/30 transition-all duration-1000 taos:opacity-0 taos:translate-y-[200px]" data-taos-offset="300" data-taos-delay="600">
          <h3 class="text-2xl font-bold mb-4">Parte del Equipo</h3>
          <div class="grid grid-cols-2 gap-4 text-sm">
            <div>
              <p class="font-semibold text-purple-400">Leonel Lopez Moccia</p>
              <p class="text-gray-400">Founder and CEO</p>
            </div>
            <div>
              <p class="font-semibold text-pink-400">Carol Mirabal</p>
              <p class="text-gray-400">IT Manager</p>
            </div>
            <div>
              <p class="font-semibold text-blue-400">Yuliana Kalyniuk</p>
              <p class="text-gray-400">Team Manager</p>
            </div>
            <div>
              <p class="font-semibold text-indigo-400">Nataliia Pasichnyk</p>
              <p class="text-gray-400">Project Manager</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }

  input:focus, textarea:focus {
    outline: none;
  }
</style>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  
  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    
    // Here you would typically send the form data to a server
    // For now, we'll just show a success message
    
    const formData = new FormData(form);
    const data = Object.fromEntries(formData);
    
    console.log('Form submitted:', data);
    
    // Show success message
    alert('¡Gracias por tu mensaje! Nos pondremos en contacto contigo pronto.');
    form.reset();
  });
</script>
